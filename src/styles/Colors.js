import { ThemeColors, useTheme } from "react-navigation";

const Colors = {
  light: {
    backgroundColor: "#fff",
    fontColor: "#000",
  },
  dark: {
    backgroundColor: "#000",
    fontColor: "#fff",
  },
};

export default Colors;
